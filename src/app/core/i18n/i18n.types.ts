export type Locale = 'es-CO' | 'en-US';

export interface Translations {
  nav: {
    home: string;
    projects: string;
    lab: string;
    contact: string;
  };
  topbar: {
    role: string;
  };
  lang: {
    toggle_label: string;
  };
}
